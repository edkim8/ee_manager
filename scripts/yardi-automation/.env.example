# CDP port — must match the --remote-debugging-port Chrome was launched with
CDP_PORT=9222

# Yardi property selector string shown on the dashboard
YARDI_PROPERTY_SELECTOR=.clawren

# Override Google Drive path if auto-detection fails (optional)
# GOOGLE_DRIVE_PATH=/Volumes/GoogleDrive/My Drive
